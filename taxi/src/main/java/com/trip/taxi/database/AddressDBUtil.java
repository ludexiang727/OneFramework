package com.trip.taxi.database;

import android.content.ContentResolver;
import android.content.ContentValues;
import android.content.Context;
import android.net.Uri;
import com.one.framework.db.DBContentProvider;
import com.one.map.model.Address;

/**
 * Created by ludexiang on 2018/6/11.
 */

public class AddressDBUtil {
//  private static final String BASE_URI = ContentResolver.SCHEME_CONTENT + "://" + DBContentProvider.AUTHORITY;
//  private static Uri sUri;
//
//  public static void insert(Context context, Address address) {
//    Uri uri = Uri.parse(ContentResolver.SCHEME_CONTENT + "://" + DBContentProvider.AUTHORITY + "/" + );
//    ContentValues values = new ContentValues();
//    context.getContentResolver().insert(uri, values);
//  }
//
//  public static void update(Context context) {

//  }
}
